#pragma once

#include <vector>
#include <map>
#include <unordered_map>
#include <queue>
#include <string>

#include <thread>
#include <mutex>

const int IDENTIFIER_SIZE = 16;
const int STATUS_SIZE = 8;
const int SIMPLE_STATUS_SIZE = 4;
const int MESSAGE_SIZE = 200;

using uint8 = unsigned char;
using uint16 = unsigned short;